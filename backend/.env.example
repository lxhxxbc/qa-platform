# 数据库连接串 — asyncpg 是异步驱动，// 后是用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/qa_platform
# JWT 密钥 — 用于签名 Token，生产环境务必换成随机字符串
SECRET_KEY=change-me-to-a-random-secret-key
# Token 有效期（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=30
